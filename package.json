import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    host: '0.0.0.0',
    hmr: true, // Changez cette valeur à false pour désactiver le rafraîchissement à chaud.
  },
  build: {
    outDir: 'dist', // Vercel déploiera par défaut depuis ce dossier.
  },
});
